name: Build OpenWrt 25.12

on:
  workflow_dispatch:
    inputs:
      build_options:
        description: '额外编译选项 (可选)'
        required: false
        default: ''
  push:
    branches: ["main", "master"]
  schedule:
    - cron: '0 2 * * 1'

env:
  TARGET: ath79
  SUBTARGET: generic
  DEVICE: csac_qca9563-csac
  BRANCH: main
  IMAGE_SIZE: 7808k

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 300
    strategy:
      matrix:
        image_type: [squashfs]

    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        submodules: recursive
        ref: ${{ env.BRANCH }}

    - name: 验证设备树文件
      id: verify_dts
      run: |
        echo "检查设备树文件..."
        if [ -f "target/linux/ath79/dts/qca9563_csac.dts" ]; then
          echo "✅ 找到设备树文件: qca9563_csac.dts"
          echo "dts_valid=true" >> $GITHUB_OUTPUT
        else
          echo "❌ 错误: 未找到设备树文件 qca9563_csac.dts"
          echo "请确保在 target/linux/ath79/dts/ 目录下有该文件"
          ls -la target/linux/ath79/dts/ || echo "设备树目录不存在"
          exit 1
        fi

    - name: 设置编译环境
      run: |
        sudo apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
          build-essential \
          ccache \
          ecj \
          fastjar \
          file \
          g++ \
          gawk \
          gettext \
          git \
          java-propose-classpath \
          libelf-dev \
          libncurses5-dev \
          libncursesw5-dev \
          libssl-dev \
          python3 \
          python3-distutils \
          python3-setuptools \
          python3-pip \
          python3-pyelftools \
          unzip \
          wget \
          rsync \
          time \
          xsltproc \
          zlib1g-dev \
          subversion \
          gperf \
          bison \
          flex \
          asciidoc \
          bash \
          bc \
          binutils \
          bzip2 \
          diffutils \
          findutils \
          libc6-dev \
          libtool \
          make \
          patch \
          perl \
          ruby \
          sed \
          tar \
          xmlto \
          xxd \
          cmake \
          pkg-config

    - name: 彻底清理编译缓存
      run: |
        echo "🗑️  清理编译缓存，避免旧配置干扰..."
        make clean && make dirclean
        rm -rf build_dir/target-* tmp/
        echo "✅ 缓存清理完成"

    - name: 配置编译参数
      run: |
        rootfs_size="${{ env.IMAGE_SIZE }}"
        rootfs_size="${rootfs_size%k}"
        if [ -f "seed.config" ]; then
          echo "使用自定义的 seed.config 配置文件"
          cp seed.config .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=${rootfs_size}" >> .config
        else
          echo "seed.config 不存在，使用默认配置"
          echo "CONFIG_TARGET_${{ env.TARGET }}=y" > .config
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}=y" >> .config
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}_DEVICE_${{ env.DEVICE }}=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=${rootfs_size}" >> .config
          echo "CONFIG_DEVICE_DTS_OVERRIDE=1" >> .config
        fi
        echo "CONFIG_DEVICE_DTS_OVERRIDE=1" >> .config
        if [ -n "${{ github.event.inputs.build_options }}" ]; then
          echo "${{ github.event.inputs.build_options }}" >> .config
        fi

    - name: 下载 feeds
      run: |
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 配置编译选项
      run: |
        make defconfig
        echo "✅ 配置完成，开始预下载依赖包..."

    - name: 提前下载所有依赖包
      run: |
        FORCE_UNSAFE_CONFIGURE=1 make download -j$(nproc)
        find dl -size -1024c -delete
        echo "✅ 所有依赖包下载完成，共$(ls dl | wc -l)个包"

    - name: 开始编译
      run: |
        echo "开始编译 ${{ env.DEVICE }}... 线程数: $(($(nproc)+1))"
        if ! time make -j$(($(nproc)+1)) V=s 2>&1 | tee build.log; then
          echo "⚠️  多线程编译失败，切换为单线程重新编译（打印详细日志）..."
          time make -j1 V=s 2>&1 | tee -a build.log
          exit 1
        fi

    - name: 检查编译结果
      run: |
        BIN_DIR="bin/targets/${{ env.TARGET }}/${{ env.SUBTARGET }}"
        if [ -d "$BIN_DIR" ]; then
          echo "✅ 编译成功！生成的固件文件："
          find "$BIN_DIR" -name "*.bin" -o -name "*.img" -o -name "*.gz" | while read file; do
            echo "- $(basename "$file")"
            ls -lh "$file"
          done
          if find "$BIN_DIR" -name "*initramfs-kernel.bin" | grep -q .; then
            echo "✅ 已成功生成 initramfs-kernel.bin 固件！"
          else
            echo "⚠️  未生成 initramfs-kernel.bin ，请检查设备配置"
          fi
        else
          echo "❌ 错误：编译失败，$BIN_DIR 目录不存在"
          echo "显示编译日志的最后200行："
          tail -200 build.log
          exit 1
        fi

    - name: 上传固件制品
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-${{ env.DEVICE }}-${{ github.sha }}-${{ matrix.image_type }}
        path: bin/targets/${{ env.TARGET }}/${{ env.SUBTARGET }}/
        retention-days: 7

    - name: 上传编译日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-log-${{ github.sha }}
        path: |
          build.log
          .config
        retention-days: 7

    - name: 通知结果
      if: always()
      run: |
        if [ -d "bin/targets/${{ env.TARGET }}/${{ env.SUBTARGET }}" ]; then
          echo "✅ 编译成功！"
          echo "可以在 Artifacts 中找到编译好的固件（含initramfs-kernel.bin）。"
        else
          echo "❌ 编译失败！"
          echo "请检查 build.log 文件中的错误信息。"
          exit 1
        fi